import 'package:flutter/material.dart';

class JobsCreatedTab extends StatelessWidget {
  const JobsCreatedTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      'Jobs created tab',
    );
  }
}

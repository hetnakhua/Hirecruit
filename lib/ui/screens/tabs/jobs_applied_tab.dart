import 'package:flutter/material.dart';

class JobsAppliedTab extends StatelessWidget {
  const JobsAppliedTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      'Jobs applied tab',
    );
  }
}

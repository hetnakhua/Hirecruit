import 'package:flutter/material.dart';

class JobsCreateTab extends StatelessWidget {
  const JobsCreateTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      'Jobs created tab',
    );
  }
}
